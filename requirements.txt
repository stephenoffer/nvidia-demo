ray[data]>=2.10.0
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0
opencv-python>=4.8.0
pillow>=10.0.0
transformers>=4.30.0
sentencepiece>=0.1.99
scikit-learn>=1.3.0
numba>=0.57.0
cupy-cuda12x>=12.0.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
plotly>=5.14.0
h5py>=3.9.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# MLOps tools (recommended for production)
mlflow>=2.8.0  # Experiment tracking and model versioning
openlineage-python>=1.0.0  # Data lineage tracking
wandb>=0.15.0  # Alternative experiment tracking (optional, use MLflow or W&B, not both)
prometheus-client>=0.19.0  # Prometheus metrics client library (replaces custom exporter)

# NVIDIA Libraries (install separately based on availability)
# Core NVIDIA libraries for GPU acceleration
cudf-cu12>=23.12.0  # RAPIDS cuDF for GPU-accelerated DataFrames
cuml-cu12>=23.12.0  # RAPIDS cuML for GPU-accelerated ML algorithms
cupy-cuda12x>=12.0.0  # GPU-accelerated NumPy (already listed above)
nvidia-ml-py>=12.0.0  # NVIDIA Management Library (pynvml) for GPU monitoring

# NVIDIA NCCL for multi-GPU communication (usually comes with PyTorch/CUDA)
# NCCL is included with PyTorch when installed with CUDA support
# For standalone installation: conda install -c nvidia nccl

# NVIDIA NeMo for language models and embeddings (optional)
# nemo_toolkit[all]>=1.20.0  # NVIDIA NeMo framework
# sentence-transformers>=2.2.0  # Fallback for embeddings if NeMo unavailable

# NVIDIA Isaac for robotics simulation (optional)
# isaac-sim>=4.2.0  # NVIDIA Isaac Sim (requires NVIDIA account)
# isaac-lab>=1.0.0  # NVIDIA Isaac Lab (open source)

# NVIDIA CUDA runtime (usually comes with CUDA toolkit)
# nvidia-cuda-runtime-cu12>=12.0.0  # CUDA runtime

